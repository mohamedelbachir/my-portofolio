---
import Timeline from "@/components/TimeLine.astro";
import type { Item } from "@/interfaces/type";
interface Props {
  items: Array<Item>;
  classes?: Record<string, any>;
}

const { items = [], classes = {} } = Astro.props;
---

<Timeline items={items} classes={classes?.items} />
